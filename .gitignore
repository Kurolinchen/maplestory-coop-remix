/logs/**
.idea/*
!.idea/codeStyles/
*.iml
/target
__pycache__/
*.py[cod]

# build files
/build/
/dist/
/nbproject/
/.settings
/out
*.onetoc2

# Eclipse m2e generated files
# Eclipse Core
.project
# JDT-specific (Eclipse Java Development Tools)
.classpath

# Database
database/docker-db-data
database/docker-pg-db-data

# macOS files
.DS_Store

# Co-op Remix dev environment
.env
ops/.env
ops/vps.env
ops/registration.env
docs/handbook/build/
ops/config/
ops/backups/

# Agent session transcripts (must never be committed; may contain secrets)
session-*.md

# Local client workspace: binaries, Nexon assets, WZ client files, builds, archives, logs.
# Only manifests/tooling/docs in the repo (see docs/CLIENT.md); never client binaries.
.local/
