# Copy to ops/.env (ops/start.sh does this automatically and generates a real password).
# NEVER commit the real .env. Values here are examples only.

# MySQL root password for the local dev database (must not be empty)
DB_ROOT_PASSWORD=change-me-strong-password
