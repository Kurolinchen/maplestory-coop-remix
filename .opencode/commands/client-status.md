---
description: Verify and report the local client runtime. Usage; /client-status
---

Run `ops/client-status.sh` and report the result concisely: true-win32 runner/prefix,
mandatory runtime manifest verification, client process, login port and server containers.

Do not install, launch, copy or modify anything. Never print client assets or secrets.

Extra focus (if any): $ARGUMENTS
